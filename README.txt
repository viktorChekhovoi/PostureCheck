The goal is to create a website that can detect if the user is slouching by analyzing images for their webcam, then notify the user to correct their posture if needed.

The current version can be ran by executing the following commands:

(in python-webcam-flask)
pip install -r requirements.txt
export FLASK_APP=app.py
flask run OR heroku local
